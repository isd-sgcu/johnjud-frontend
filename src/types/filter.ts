interface filterState {
  dog: boolean;
  cat: boolean;
  male: boolean;
  female: boolean;
  white: boolean;
  black: boolean;
  brown: boolean;
  blonde: boolean;
  minAge: number;
  maxAge: number;
}

export type { filterState };
