const BigPetCardContainer = () => {
  return (
    <div className="flex space-x-9 overflow-x-auto">
      {/* petsData.map() here*/}
      {/* on mobile view we have to render only one pet card*/}
    </div>
  );
};
export default BigPetCardContainer;
